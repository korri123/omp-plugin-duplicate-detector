{
  "name": "omp-plugin-duplicate-detector",
  "version": "0.1.0",
  "description": "Code clone and duplication detector plugin for oh-my-pi powered by jscpd",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "check": "tsc --noEmit",
    "check:types": "tsc --noEmit",
    "test": "bun test"
  },
  "keywords": [
    "oh-my-pi",
    "omp",
    "omp-plugin",
    "omp-extension",
    "duplicate-detector",
    "jscpd",
    "code-clone",
    "refactoring"
  ],
  "license": "MIT",
  "omp": {
    "name": "duplicate-detector",
    "description": "Detect duplicate and clone code blocks across your codebase powered by jscpd",
    "extensions": [
      "./src/index.ts"
    ],
    "settings": {
      "minLines": {
        "type": "number",
        "description": "Minimum consecutive lines required to report a duplicate block",
        "default": 5
      },
      "minTokens": {
        "type": "number",
        "description": "Minimum token count threshold for code clone detection",
        "default": 40
      },
      "checkOnMutation": {
        "type": "boolean",
        "description": "Automatically check newly written/edited code for duplicates",
        "default": true
      },
      "reminderMode": {
        "type": "enum",
        "enum": [
          "in-band",
          "steer",
          "none"
        ],
        "description": "Feedback style: in-band (<system-reminder> on tool_result), steer (follow-up message), or none",
        "default": "in-band"
      },
      "ignorePatterns": {
        "type": "string",
        "description": "Comma-separated glob patterns to ignore during duplicate detection",
        "default": "node_modules/**,dist/**,build/**,.git/**,coverage/**"
      }
    }
  },
  "dependencies": {
    "eventemitter3": "^5.0.4",
    "spark-md5": "^3.0.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "peerDependencies": {
    "@oh-my-pi/pi-coding-agent": ">=18.0.0"
  },
  "peerDependenciesMeta": {
    "@oh-my-pi/pi-coding-agent": {
      "optional": true
    }
  }
}
